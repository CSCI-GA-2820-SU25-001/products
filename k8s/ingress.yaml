apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: products
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: products
                port:
                  number: 8080
