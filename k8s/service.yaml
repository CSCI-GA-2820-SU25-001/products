apiVersion: v1
kind: Service
metadata:
  name: products-devcontainer
spec:
  type: ClusterIP
  selector:
    app: products-devcontainer
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080